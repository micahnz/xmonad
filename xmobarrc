Config {
	font = "-misc-fixed-*-*-*-*-10-*-*-*-*-*-*-*",
	bgColor = "black",
	fgColor = "grey",
	position = Top,
	lowerOnStart = True,
	commands = [
		Run Cpu ["-t","<total>%"] 10,
		Run Memory ["-t","<usedratio>%"] 100,
		Run Date "%d %b %Y - %H:%M" "date" 10,
		Run Wireless "wlan0" ["-t", "<quality>%"] 100,
		Run Battery [
			"-t", "<left>%",
			"-h", "green",
			"-n", "yellow",
			"-l", "red"
		] 10,
		Run StdinReader
	],
	sepChar = "%",
	alignSep = "}{",
	template = "%StdinReader%}{%cpu%|%memory% - %wlan0wi% <fc=#FFF>%date%</fc> %battery%"
}
